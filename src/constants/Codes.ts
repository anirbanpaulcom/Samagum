import { Errors } from './ErrorMessages';

const zero = { code: '0', message: Errors.offlineError };
const connection = { code: '', message: Errors.offlineError };
// const unknown = { code: 'unknown', message: Errors.sww };

export {
  zero,
}